**1**
paths used for sources are relative to the CMakeLists.txt file location

**2**
Ninja is a build system, while CMake is a build system generator

**3**
Building generates a lot of files so having a build directory helps keep them separate from source files and keep the repo clean